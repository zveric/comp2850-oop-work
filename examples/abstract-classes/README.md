Put `abstraceClassBasic.kt` in a directory.

Make sure you have run
    module load kotlinc

Compile the program with

    kotlinc abstraceClassBasic.kt

Run the program with

    kotlin abstraceClassBasic