Put `interfaesBasic.kt` in a directory.

Make sure you have run
    module load kotlinc

Compile the program with

    kotlinc interfaesBasic.kt

Run the program with

    kotlin HelloKtinterfaesBasic